<Page
    x:Class="RandomUWP.NumMode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RandomUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Grid Padding="24">
        <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch" Padding="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="ui_text_min" Grid.Row="0" Grid.Column="0" Margin="10" Text="最小值" VerticalAlignment="Center" FontSize="24"/>
            <muxc:NumberBox x:Name="ui_numbox_min" Grid.Row="0" Grid.Column="1" Margin="10" SpinButtonPlacementMode="Inline" VerticalAlignment="Center" HorizontalAlignment="Right" Height="50" Width="350" Value="1" FontSize="24"/>

            <TextBlock x:Name="ui_text_max" Grid.Row="1" Grid.Column="0" Margin="10" Text="最大值" VerticalAlignment="Center" FontSize="24"/>
            <muxc:NumberBox x:Name="ui_numbox_max" Grid.Row="1" Grid.Column="1" Margin="10" SpinButtonPlacementMode="Inline" VerticalAlignment="Center" HorizontalAlignment="Right" Height="50" Width="350" Value="57" FontSize="24"/>

            <TextBlock x:Name="ui_text_times" Grid.Row="2" Grid.Column="0" Margin="10" Text="次数" VerticalAlignment="Center" FontSize="24"/>
            <muxc:NumberBox x:Name="ui_numbox_times" Grid.Row="2" Grid.Column="1" Margin="10" SpinButtonPlacementMode="Inline" VerticalAlignment="Center" HorizontalAlignment="Right" Height="50" Width="350" Value="1" FontSize="24"/>
        </Grid>
        <TextBlock x:Name="result_text" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap" Text="Result" FontSize="48" Visibility="Collapsed"/>
        <Button x:Name="start_button" Content="Random!" Margin="24" VerticalAlignment="Bottom" Height="65" Width="300" Click="start_button_Click" HorizontalAlignment="Right"/>
        <Button x:Name="next_button" Content="Next" Margin="24" VerticalAlignment="Bottom" Height="65" Width="300" HorizontalAlignment="Right" Visibility="Collapsed" Click="next_button_Click"/>
        <Button x:Name="finish_button" Content="Finish" Margin="24" VerticalAlignment="Bottom" Height="65" Width="300" HorizontalAlignment="Right" Visibility="Collapsed" Click="finish_button_Click"/>

    </Grid>
</Page>
